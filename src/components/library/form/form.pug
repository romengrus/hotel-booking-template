//- Form component
//- mods  {array} Modification classes
mixin form(props = {})
  -
    props = Object.assign({
      mods: []
    }, props)

    var classList = ['form'].concat(props.mods)

  form(class= classList)&attributes(attributes)
    block

