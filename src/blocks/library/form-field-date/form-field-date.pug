//- Date input component
//- mods  {array} Modifier classes
mixin form-field-date(params = {})
  include /blocks/library/form-field/form-field.pug

  -
    params = deepmerge({
      mods: [],
    }, params)

    attributes = deepmerge({
      type: 'text', 
    }, attributes)

    var classList = ['form-field-date'].concat(params.mods)

  +form-field({mods: classList})&attributes(attributes)